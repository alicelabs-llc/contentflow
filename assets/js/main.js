/* ============================================
   CONTENTFLOW AI — Main JS Entry Point
   ============================================
   Loads all modules after DOM is ready.
   ============================================ */

document.addEventListener('DOMContentLoaded', () => {
  console.log('ContentFlow AI — v2.0 initialized');
});
